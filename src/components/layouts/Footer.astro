---
import ThemeSelect from '@/components/theme-select';
import SocialLinks from '@/components/social-links';
import slateConfig from '~@/slate.config';

interface Props {
  copyright?: string;
}

const { copyright = slateConfig.footer?.copyright } = Astro.props;
---

<footer class="mt-auto">
  {!!slateConfig.socialLinks && <SocialLinks client:visible />}
  <div class="mt-4 flex items-center justify-between">
    <div class="text-slate8 text-sm">{copyright}</div>
    {slateConfig.theme?.enableUserChange && <ThemeSelect client:load />}
  </div>
</footer>
